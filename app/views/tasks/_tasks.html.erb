<ul class="media-list">
  <% tasks.each do |task| %>
    <% user = task.user %>
    <li class="media">
      <div class="media-left">
        <img class="media-object img-rounded" src="<%= gravatar_url(user, options = { size: 50 }) %>" alt="">
      </div>
      <div class="media-body">
        <div>
          <%= link_to task.id, task_path(task) %> <span class="text-muted">modifyed at <%= task.created_at %></span>
        </div>
        <div>
          <p><%= task.content %></p>
          <p><%= task.status %></p>
        </div>
      </div>
    </li>
  <% end %>
</ul>